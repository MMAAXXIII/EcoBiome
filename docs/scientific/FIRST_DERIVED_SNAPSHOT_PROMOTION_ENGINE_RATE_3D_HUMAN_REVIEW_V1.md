# EcoBiome — RATE-3D Human Review Decision

Decision:

```text
REVISE
```

Reviewed at:

```text
2026-08-27T01:49:35+02:00
```

Canonical decision SHA-256:

```text
b29915795b57f36c62a3a883d1599065bf0a8a3bccc4f03c3e6e5080723e2598
```

Accepted in principle:

```text
replay manifest structure
32-row scope
knowledge_sources +3 dependency closure
no KnowledgeSynthesis
no SourceLineageEdge
exact raw CAS source identities
protected source-text reconstruction
candidate + identity-binding pattern
atomic complete-pair publication objective
```

Required before any execution authorization:

```text
1. ArtifactMissingError-specific derived-CAS handling
2. explicit replay payload SHA transport and validation
3. full reviewed authorization + identity binding before effects
4. supported Windows write-through publication strategy
5. end-to-end promotion-path tests
```

Authorization remains:

```text
scientific staging DB = false
snapshot creation = false
derived CAS write = false
active pointer = false
real V6 write = false
remote write = false
```
